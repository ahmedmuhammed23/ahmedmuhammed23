on:
  workflow_dispatch:
    inputs:
      environment:
        description: this is for environment you will deploy on
        required: true
        default: staging
        type: choice
        options:
          - staging
          - dev
          - predeploy
          - deploy


jobs:
  _1st_job:
    name: my first job
    runs-on: ubuntu-latest
    steps:
      - name: hello 
        run: echo "Hello from ${{github.job}}"

  
